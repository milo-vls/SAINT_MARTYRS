function main_deck_to_exploration()
{
	add_menu(new MainDesKPointsOfInterest(visitable_rooms()));
	//change_room(rm_dev, new RoomTransitionStyle(ROOM_TRANSITION_STYLES.FADE), start_exploration_menu);
}