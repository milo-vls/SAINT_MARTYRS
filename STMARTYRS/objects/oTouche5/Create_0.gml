/// @description Insérez la description ici
// Vous pouvez écrire votre code dans cet éditeur

// Inherit the parent event
event_inherited();

event = function()
{
	oCall.currentNum = string_insert("5", oCall.currentNum, string_length(oCall.currentNum)+1);
}